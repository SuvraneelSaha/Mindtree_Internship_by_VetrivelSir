package Day4;

public class O13 {
    /*

    select now();
    select curdate();
    select curtime();
    this above methods are for checking time

    // Subqueries
    it is an combination of two queries
    firstly the inner query will executed
    then secondly the outer query will be executed

    ex of a subquery -
    select * from p1 where id in ( select idnew_table from new_table);
    this is first execute the inner query and get it executed then it will
    execute the outer query

    another subquery example
    insert into p1(id) select idnew_table from new_table where idnew_table=4;
    the p1 table will have values from the post tables


    



     */
}
