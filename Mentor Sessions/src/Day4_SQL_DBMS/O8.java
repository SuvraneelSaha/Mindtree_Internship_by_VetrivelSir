package Day4_SQL_DBMS;

public class O8 {
    /*
    show databases;
     create database lti;
     use sakila;
     show tables;
     then we use
     select * from actor;
     use lti;
     show tables;
     // bottom point is you need to remember which datbase you are working with
     create table dummy(c1 int , c2 varchar(20));
     insert into dummy2 values(1,'one');

     // we will learn on how to truncate a table ;
     truncate table dummy2;
     // for removing the data from the table ;

     // alter the table ;
     alter table dummy2 drop c2;
     // for altering the table structure we use the alter command


// drop the table
drop table dummy2;
// for removing the entire table from the database ;
another example - drop database lti;

what is the difference drop and truncate
drop - deletes the whole table or the database
in case of truncate command - it deletes the data from the table



     */
}
